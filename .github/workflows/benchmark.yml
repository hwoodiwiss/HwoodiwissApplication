name: benchmark

on:
  issue_comment:
    types: [created]

permissions: {}

env:
  DOTNET_GENERATE_ASPNET_CERTIFICATE: false
  DOTNET_MULTILEVEL_LOOKUP: 0
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
  DOTNET_SYSTEM_CONSOLE_ALLOW_ANSI_COLOR_REDIRECTION: 1
  FORCE_COLOR: 1
  NUGET_XMLDOC_MODE: skip
  TERM: xterm

jobs:
  parse-and-start:
    runs-on: ubuntu-latest
    if: |
      github.event.repository.fork == false &&
      github.event.issue.pull_request != '' &&
      startsWith(github.event.comment.body, '/benchmark')
    outputs:
      commit-sha: ${{ steps.parse.outputs.commit-sha }}
    permissions:
      contents: read
      pull-requests: write
      statuses: write

    steps:
      - name: Parse comment and set status
        id: parse
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const issueNumber = context.payload.issue.number;
            const commenterId = context.payload.comment.user.login;

            try {
              await github.rest.repos.checkCollaborator({ owner, repo, username: commenterId });
            } catch {
              core.setFailed(`@${commenterId} is not a collaborator — ignoring /benchmark comment.`);
              return;
            }

            const pr = await github.rest.pulls.get({ owner, repo, pull_number: issueNumber });
            const sha = pr.data.head.sha;
            core.setOutput("commit-sha", sha);

            await github.rest.issues.createComment({
              owner, repo, issue_number: issueNumber,
              body: `Benchmark run started for ${sha}. Results will be posted here when complete.`,
            });

            await github.rest.repos.createCommitStatus({
              owner, repo, sha,
              state: "pending",
              context: "benchmark",
              description: "Benchmark run in progress...",
            });

  benchmark:
    runs-on: ubuntu-latest
    needs: parse-and-start
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.parse-and-start.outputs.commit-sha }}
          persist-credentials: false

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: ./global.json

      - name: Install .NET Workloads
        run: dotnet workload restore

      - name: Run benchmarks
        shell: pwsh
        run: ./build/benchmark.ps1

      - name: Upload BenchmarkDotNet artifacts
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: benchmarks
          path: ./artifacts/bin/HwoodiwissApplication.Benchmarks/release**/BenchmarkDotNet.Artifacts/**

  post-result:
    runs-on: ubuntu-latest
    needs:
      - parse-and-start
      - benchmark
    if: always() && needs.parse-and-start.result == 'success'
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    steps:
      - name: Post result
        uses: actions/github-script@v9
        with:
          script: |
            const { owner, repo } = context.repo;
            const issueNumber = context.payload.issue.number;
            const sha = "${{ needs.parse-and-start.outputs.commit-sha }}";
            const success = "${{ needs.benchmark.result }}" === "success";

            await github.rest.issues.createComment({
              owner, repo, issue_number: issueNumber,
              body: success
                ? `Benchmark run for ${sha} completed successfully. See the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for results.`
                : `Benchmark run for ${sha} failed. See the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details.`,
            });

            await github.rest.repos.createCommitStatus({
              owner, repo, sha,
              state: success ? "success" : "failure",
              context: "benchmark",
              description: success ? "Benchmark run completed." : "Benchmark run failed.",
            });
