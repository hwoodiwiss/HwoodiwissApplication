# HwoodiwissApplication

This is a standardized wrapper over WebApplication that preconfigures logging, configuration sources, and dependency injection to provide a consistent starting point for building ASP.NET Core applications.
